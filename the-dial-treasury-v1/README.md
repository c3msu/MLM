# The Dial Treasury

`the-dial-treasury-v1/` is the active MarcoMonitor runtime: a local US Treasury
factor dashboard with public-source data refresh, dashboard slice APIs, SQLite
history, and optional daily background updates.

## Scope

- Single-page US Treasury factor dashboard.
- Anchor navigation matching the target sections:
  summary, yield curve, decomposition, scorecard, policy, supply, positioning,
  cross-market, events/news, and investment view.
- Hand-drawn SVG charts with no external charting dependency.
- Editable factor scorecard:
  - factor score buttons from `-2` to `+2`
  - group weight inputs
  - live composite duration stance
  - live curve stance
  - Top Drivers and Module Pulse summaries derived from weighted scorecard
    contributions
  - Conclusion Audit panel that discounts proxy, modeled, and manual-placeholder
    evidence before assigning conclusion confidence and weight-change guidance
  - local persistence through `localStorage`
- The Dial-style historical context panel with factor percentile trend lines,
  percentile momentum, and extreme-percentile alerts. The chart shows a compact
  3-year history while each point is scored against a rolling 5-year window.
- Bhadial/The Dial-compatible Conditions Score:
  30 scored factors across Liquidity, Funding, Treasury, Rates, Credit, Risk,
  and External modules are converted into 5-year historical percentile scores.
  The module roll-up uses public factor-coverage weights with overlap
  adjustment, Funding uses EMA(5), and target-distance, shock-only, deviation,
  and bounded risk-signal methods follow the public bhadial methodology. The
  payload also records the current public benchmark score from
  `https://bhadial.com/dashboard` when reachable. The final 0-100 score reads
  higher as a more supportive macro backdrop. ETF-exact factors use documented
  public proxies until a stable local ETF history feed is available.
- Generated `data/dashboard.json` from public data sources, with a static
  fallback for direct `file://` opening.
- Local SQLite history persistence in `data/history.sqlite3`, storing full
  refresh snapshots and normalized key metrics for trend/backfill work.
- Five-year public historical series backfill into SQLite for Treasury curve,
  FRED macro/liquidity/risk series, derived spreads, and TreasuryDirect
  bid-to-cover history.
- Interactive historical statistics panel with 1Y/3Y/5Y range controls,
  series selector, hover tooltip, and min/max/quantile summaries.
- Cross-market historical dynamics panel in section 07, with group controls
  for global rates, risk/dollar, and inflation/commodities, plus a per-series
  1Y/3Y/5Y history chart backed by the SQLite history API.
- Section 09 investment-view cards generated by deterministic, scenario-aware
  rules for duration, 5s30s curve, front-end carry, and breakeven inflation;
  card thesis text remains editable in-browser and can be replaced through
  `content/overrides.json`. Cards also display a compact confidence overlay
  from `conclusionAudit` so weaker evidence is visible beside the trade view,
  plus a historical S&P 500 price-index proxy for SPY impact block based on
  matching Conditions Score level and 3-month score-change buckets.
- Local REST API routes when using `scripts/serve.py`.
- Daily background update entrypoints through the local Python server or macOS
  LaunchAgent.
- Chinese/English UI shell toggle with local persistence. Public-source data
  fields remain source-faithful when no safe translation is available.

## Run

Direct file opening works as a fallback:

```text
file:///.../the-dial-treasury-v1/index.html
```

Real-data mode requires the local server so the page can fetch
`data/dashboard.json` and expose the API routes:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=the-dial-treasury-v1 \
  python3 the-dial-treasury-v1/scripts/serve.py --skip-start-update --port 8451
```

Then open:

```text
http://127.0.0.1:8451/
```

## Daily Background Update

Run the dashboard server with a daily local-time refresh:

```bash
python3 the-dial-treasury-v1/scripts/serve.py --port 8451 --daily-at 16:30
```

The server serves the existing `the-dial-treasury-v1/data/dashboard.json`
immediately, runs the startup refresh in the background, and then refreshes
again every day at the configured `HH:MM`. After each scheduled daily refresh,
the server also refreshes the public historical SQLite backfill for the last
5 years unless `--history-years 0` is passed. Refresh writes are atomic, so API
requests do not read a partially written dashboard file. If a refresh returns
real-source `error` rows while the current dashboard is healthy, the service
keeps serving the last healthy dashboard and writes the failed candidate to
`the-dial-treasury-v1/data/dashboard.failed.json` for inspection.

The hero `刷新` button triggers `POST /api/update`, which starts the same
public-data refresh in a background thread and returns the current snapshot
immediately. The server must be running through `scripts/serve.py`; direct
`file://` mode can only show the embedded fallback snapshot.
If another manual refresh is already running, the endpoint returns `running`
and reuses the active thread instead of starting a duplicate fetch.
Every successful startup, scheduled, CLI, or manual refresh also persists the
full dashboard snapshot and key metric observations into
`the-dial-treasury-v1/data/history.sqlite3`. Failed refresh candidates that are
rejected in favor of the last healthy dashboard are not written to history.

To seed or refresh the five-year historical series immediately:

```bash
python3 the-dial-treasury-v1/scripts/backfill_history.py --years 5
```

The backfill writes idempotent daily observations into the same SQLite database,
so rerunning it updates existing rows instead of duplicating history.

For a macOS login background service, write a LaunchAgent plist:

```bash
python3 the-dial-treasury-v1/scripts/install_launch_agent.py --daily-at 16:30 --port 8451 --load
```

Install the companion health checker after the refresh window:

```bash
python3 the-dial-treasury-v1/scripts/install_health_agent.py --daily-at 16:45 --port 8451 --load
```

Manual health check:

```bash
python3 the-dial-treasury-v1/scripts/check_health.py --url http://127.0.0.1:8451/api/health
```

## Data Boundary

Current real public sources:

- U.S. Treasury daily yield curve XML.
- 10Y realized-rate volatility proxy computed from U.S. Treasury daily yield
  curve history.
- FRED CSV series: `DFII10`, `T10YIE`, `DFF`, `SOFR`, `WTREGEN`, `WALCL`,
  `TREAST`, `WRESBAL`, `RRPONTSYD`, `CPIAUCSL`, `PCEPI`, `PCEPILFE`,
  `PCETRIM12M159SFRBDAL`, `PPIACO`, `UNRATE`, `PAYEMS`,
  `GDPC1`, `SP500`, `VIXCLS`, `DTWEXBGS`, `BAMLH0A0HYM2`, `BAMLC0A0CM`,
  `IRLTLT01JPM156N`, `IRLTLT01DEM156N`, `IRLTLT01GBM156N`, `DCOILWTICO`,
  `OVXCLS`, `GVZCLS`.
- The Dial-style historical percentiles for public series where history is
  available: bank reserves, net liquidity (`WALCL - WTREGEN - RRPONTSYD`),
  liquidity momentum, SOFR-EFFR spread, VIX, HY OAS, broad dollar, and auction
  bid-to-cover. The same payload now includes sampled 3-year history,
  percentile momentum, and threshold alerts for the scorecard trend panel.
- Cross-market history registry under `cross.historySeries`, covering global
  10Y yields, SPX/VIX/dollar/credit spreads, CPI/PCE/core PCE/Dallas Fed
  Trimmed Mean PCE, breakevens/real yields, WTI, OVX, and GVZ. The UI reads
  this registry and queries `/api/history/series` by `category`, `name`, and
  `label`.
- The headline Conditions Score uses the same public-source percentile
  machinery across 30 scored factors and 7 bhadial modules. High funding,
  credit, volatility, dollar, and energy stress are inverse-scored or
  shock-scored when high values indicate a less supportive backdrop.
- The duration/curve conclusion audit uses the editable scorecard, not the
  0-100 Conditions Score. Individual factor contribution is `factor score *
  normalized module weight / factor count`; source modes then discount evidence
  quality so proxy and modeled inputs cannot silently produce high-confidence
  conclusions.
- TreasuryDirect auctioned and announced securities JSON, including
  bid-to-cover percentile scoring over the available endpoint sample. If the
  live auctioned endpoint times out, the builder falls back to the latest
  SQLite backfilled bid-to-cover history instead of blanking the auction panel.
- U.S. Treasury Quarterly Refunding documents and press releases: latest QRA
  quarter, policy statement, financing estimates, next release dates, borrowing
  estimates, TGA/cash-balance assumptions, coupon/bill stance, and buyback size.
- Treasury Fiscal Data Daily Treasury Statement `Debt Subject to Limit` API for
  statutory debt limit, debt subject to limit, and remaining headroom.
- Federal Reserve FOMC calendar page, with the Federal Reserve Bank of Chicago
  FOMC schedule page as an official-system fallback when the Board page is
  temporarily unreachable.
- FRED economic release calendar pages for BLS CPI, PPI, and Employment
  Situation release dates.
- BEA release schedule page for GDP and Personal Income/Outlays release dates.
- Federal Reserve SEP projection materials page for official fed-funds-rate
  medians.
- U.S. Treasury press releases and Federal Reserve press-release RSS for
  recent official news headlines where reachable.
- Stooq public quote CSV for continuous 30-Day Fed Funds futures `ZQ.F`, used
  as a public futures proxy for the Fed path model.
- Stooq public quote CSV for gold spot `XAUUSD`, used in the cross-market
  inflation/commodity panel.
- NY Fed ACM term premium Excel (`ACMTP10`, `ACMRNY10`).
- NY Fed Markets API primary dealer statistics, including Treasury dealer
  positions, transactions, repo financing, borrowed securities, and lent
  securities where disclosed.
- CFTC financial futures COT historical zip for Treasury futures positioning.
- Treasury TIC table 5 text for major foreign Treasury holders.

Current modeled or placeholder fields:

- Fed path probabilities use public `ZQ.F` Fed Funds futures plus curve/macro
  pressure as a transparent model, not official CME FedWatch probabilities.
- The SEP dot plot field is official but low frequency; if the Federal Reserve
  Board site is temporarily unreachable, it is surfaced as a warning rather
  than blocking the daily refresh. The curve-implied path beside it remains the
  dashboard's transparent model proxy until CME/OIS data is supplied.
- Official Treasury/Fed headlines are fetched as public news. Full-text
  real-time market news, Reuters/Bloomberg-style redistribution, and broader
  private event calendars still require authorized feeds or manual overrides.
- MOVE index itself, market depth, bid-ask, swaps, futures basis, and
  on/off-the-run spread data remain paid/licensed-data boundaries unless a feed
  is provided. The dashboard uses a public 10Y realized-volatility proxy from
  Treasury curve history when MOVE is unavailable.
- Primary dealer data is real NY Fed weekly data, but it is voluntarily
  reported and not real-time.

The page shows the data mode and generated timestamp in the hero. If it is
opened directly as `file://`, it uses the embedded static fallback by design.

## API

When served through `scripts/serve.py`, the same `dashboard.json` is exposed as
REST slices:

- `/api/dashboard`
- `/api/health`
- `/api/curve`
- `/api/decomposition`
- `/api/fed_path`
- `/api/scorecard`
- `/api/policy`
- `/api/auctions`
- `/api/positioning`
- `/api/cross`
- `/api/percentiles`
- `/api/events`
- `/api/news`
- `/api/ideas`
- `/api/source_status`
- `/api/source-status`
- `/api/history`
- `/api/history/snapshots`
- `/api/history/stats`
- `/api/history/series`
- `POST /api/update`

List-style endpoints that carry ISO dates, such as `/api/events`, accept
`from=YYYY-MM-DD` and `to=YYYY-MM-DD`. Any API route can be exported as CSV
with `format=csv`; curve exports are transposed by tenor.
`/api/health` returns `ok` or `degraded` plus source-status counts and update
timestamps for local monitoring. It also includes the SQLite history summary
and latest 5-year history backfill run. If the latest backfill has source
errors, those errors are surfaced as health warnings so the daily health
checker can notify on background-history issues.
`POST /api/update` starts a background refresh and returns `202 accepted` with
the current `asOf` and `generatedAt`; repeated calls while it is active return
`running` without launching duplicate source fetches.
`/api/history` returns SQLite history counts and latest snapshot metadata;
`/api/history/snapshots` returns recent stored snapshot metadata.
`/api/history/stats` returns per-series count, date range, latest value,
min/max, mean, and P10/P50/P90 quantiles. `/api/history/series` accepts
`category`, `name`, optional `label`, `years`, and `limit` query parameters and
returns sampled chronological points for the interactive history chart.
`/api/cross` includes the `historySeries` registry used by section 07 to
request cross-market history without hard-coding UI-only series lists.

The update script writes `data/dashboard.json` and, by default,
`data/history.sqlite3`; API slices are served dynamically by the local Python
server. Use `--no-history` only for one-off test refreshes that should not be
persisted. See `../docs/treasury-deployment.md`.

## Verification

Run these checks before treating app or documentation changes as ready:

```bash
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=the-dial-treasury-v1 \
  python3 -m unittest discover -s the-dial-treasury-v1/tests

node --check the-dial-treasury-v1/i18n.js
node --check the-dial-treasury-v1/app.js

PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=the-dial-treasury-v1 \
  python3 the-dial-treasury-v1/scripts/smoke_check.py \
  --path the-dial-treasury-v1/data/dashboard.json
```

## Manual Content Overrides

Copy `content/overrides.example.json` to `content/overrides.json` to override
manual narratives, events/news, ideas, group weights, or factor scores during
the next data build.
